import React from "react";
import './adSpace.sass'

const FirstAdSpace = ({ data }) => {
  //Data is allShopifyProduct
  return (
    <div className="first-ad-space" style={{ margin: "0", }}>
        Sign up and save 15%
    </div>
  );
};

export default FirstAdSpace;